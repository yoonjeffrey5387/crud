# Configuración de Base de Datos MariaDB
# Según las instrucciones del proyecto universitario

# MariaDB local (configuración principal)
db.url=jdbc:mariadb://localhost:3306/agenda
db.user=usuario1
db.password=superpassword
db.driver=org.mariadb.jdbc.Driver

# H2 en memoria (comentado - solo para desarrollo local)
# db.url=jdbc:h2:mem:agenda;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
# db.user=sa
# db.password=
# db.driver=org.h2.Driver

# (Solo para pruebas) H2 en memoria en modo MySQL
test.db.url=jdbc:h2:mem:agenda;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
test.db.user=sa
test.db.password=
test.db.driver=org.h2.Driver
